def insertatEnd(head,x):
    temp = Node(x)
    if head = None:
        return temp
    else:    
        curr = head
        while curr.next!=None:
            curr = curr.next
        curr.next = temp
        temp.prev = curr
        
    return head
    
  #time - theta(n)
  #if we maintain a tail pointer we can delete last node in constant time
    
    
    
