def binarySearch(l,x,low,high):
    low = 0
    high = len(l)-1
    if low > high:
        return -1
    mid = (low+high)//2
    if x == l[mid]:
        return mid 
    if x > l[mid]:
        retrun binarySearch(l,x,mid + 1,high)
    else:
        return binarySearch(l,x,low,mid - 1)
    
