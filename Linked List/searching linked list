Class Node:
    def __init__(self,key):
        self.key = key
        self.next = None
      
def search(head,x):
    pos = 1
    curr = head
    
    while curr !=None:
        if curr.key == x
            return pos
        pos+=1
        curr = curr.next
        
    return -1
