class Node:
    def __init__(self,data):
        self.data = data
        self.next = None
     
def printList(head):
    curr = head
    while (curr != None):
        print(curr.data)
        curr = curr.next
        
        
        #time - theta(n)
        
